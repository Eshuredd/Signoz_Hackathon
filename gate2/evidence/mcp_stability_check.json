{
  "captured_at": "2026-07-25T07:39:00Z",
  "excluded_values": [
    "retrieved_at"
  ],
  "failed_stage": "mcp_search_result_parsing",
  "reason": "MCP trace tool calls returned upstream 403 authorization failures, so no repeated structured trace retrieval workflow could run.",
  "required_comparison": [
    "same trace ID",
    "same span count",
    "same span IDs",
    "same parent-child relationships",
    "same span names",
    "same required field availability",
    "same attribute keys",
    "same resource attribute keys"
  ],
  "sanitized": true,
  "stability_result": "unavailable"
}
